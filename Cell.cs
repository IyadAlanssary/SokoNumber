namespace HelloWorld
{
    class Cell
    {
        public int x, y;
        public string value;
        public Cell(string v, int x, int y)
        {
            this.value = v;
            this.x = x;
            this.y = y;
        }
    }
}